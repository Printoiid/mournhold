# Hyprland - Task File
- name: Hyprland - Install and Configure
  tags: hyprland-setup

  block:
    - name: Clear config directory
      ansible.builtin.file:
        dest: "{{ hyprland_config_path }}/*"
        state: absent
